import React from 'react';

class StudentClass extends React.Component {

    //render is used to return class component
render() {
    return (
    <h1>Welcome to React{this.title}</h1>
    );
}
}

export default StudentClass;